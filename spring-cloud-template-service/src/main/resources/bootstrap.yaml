server:
  port: 8080
spring:
  application:
    name: service
  cloud:
    config:
      discovery:
        serviceId: config
        enabled: true
      username: config
      password: config
eureka:
  client:
    service-url:
      defaultZone: http://eureka:eureka@localhost:8761/eureka
security:
  user:
    name: service
    password: service