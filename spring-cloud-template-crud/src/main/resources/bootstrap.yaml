server:
  port: 8083
spring:
  application:
    name: crud
  cloud:
    config:
      discovery:
        serviceId: config
        enabled: true
      username: config
      password: config
eureka:
  client:
    service-url:
      defaultZone: http://eureka:eureka@localhost:8761/eureka